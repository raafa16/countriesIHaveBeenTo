<!-- app/views/shared/_navigation.html.erb -->
<nav class="navbar navbar-expand-lg navbar-light bg-light mb-5">
  <div class="container">
    <%= link_to "Countries I Have Visited To", root_path, class: "navbar-brand", data: { turbolinks: false } %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <% if admin_signed_in? %>
            <%= link_to('Logout', destroy_admin_session_path, method: :delete, class: "nav-link") %>
          <% else %>
            <%= link_to('Login', new_admin_session_path, class: "nav-link") %>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>